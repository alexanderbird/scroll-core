const { identifier, reference } = require('./core/identifier');
const { jump } = require('./core/jump');
const shortIdentifier = require('./core/shortIdentifier');

module.exports = { reference, identifier, jump, shortIdentifier };
